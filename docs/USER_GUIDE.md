# User Guide

## First screen

Open `/dashboard` after local setup. The visible status strip must show:

- `PAPER` or `LIVE`.
- `READ ONLY` or trading mode.
- Data feed.
- Market open/closed.

In this version, all trading actions are intentionally unavailable.
